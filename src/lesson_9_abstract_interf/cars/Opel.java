package lesson_9_abstract_interf.cars;

public abstract class Opel extends Car{
    public Opel(String model, int maxSpeed, int productionYear) {
        super(model, maxSpeed, productionYear);
    }
}
